<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EzHRMApp.Views"
             xmlns:ViewModel="clr-namespace:ViewModel;assembly=ViewModel" x:Class="EzHRMApp.Views.StaffsView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        
        <ListView ItemsSource="{Binding EmployeeList}">
            <ListView.DataContext>
                <ViewModel:StaffsViewModel/>
            </ListView.DataContext>
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="140" Header="a" DisplayMemberBinding="{Binding Ten}"/>
                </GridView>
            </ListView.View>
        </ListView>        

        <StackPanel Grid.Row="1">
            <Label Content="{Binding ViewName}"
                   Foreground="AliceBlue"/>
        </StackPanel>
    </Grid>
</UserControl>
